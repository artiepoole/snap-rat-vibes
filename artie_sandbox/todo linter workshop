go -> yamlfmt
`go`
need go in path `$HOME/go/bin`
`go install github.com/google/yamlfmt/cmd/yamlfmt@latest`
we want go and for yamlfmt



how to add a list for available channels

Can we add a list of available channels, and also let the user write a custom one
Can we refresh on state changes, or add a refresh button if it's not easy
Can we add a way to sort results by alphabetical/reverse alpha and/or other things we have information for that make sense like "latest changes"
Can we poll the snap status to see progress when installing a snap?
Can we get a way to view current changes to snaps?
Can we make left arrow and right arrow also work like escape and enter

Please make snap find or similar to allow the search to search an exact match for the current search string -
e.g. xilinx-kernel should find the unlisted but it is available publically
Please make search show installed snaps which match the search on top always
It would be great if there was an optional sidebar which overlays on top of (if too small, or is split else) for all active changes
if the terminal supports it, use ratatui-image to display the snap icon while browsing snaps. If ratatui image can't draw, then don't, I suppose
can we add the size of the snap (download-size or size(?) for uninstalled or installed-size for installed )


we need to get local snaps's icons - it would be better to use pixel map if possible as we currently are getting half blocks in tilix
we want to show connections in the manage window for installed snaps and whether they are connected or not - with a way to connect or disconnect them




for installed snaps, I want to add a button to show the difference between any versions by showing a list of file diffs between the two revisions (as seen in /snap/<name>/<rev>)
